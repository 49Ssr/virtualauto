# Repository-owned Blender assets

This directory is reserved for small, original regression assets that satisfy
`docs/BINARY_ASSET_POLICY.md`. Prefer deterministic builders in
`blender/scripts/`; no binary asset is currently committed.
