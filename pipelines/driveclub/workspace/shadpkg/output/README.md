# ShadPKG output

Private accessible package contents. The next stage needs the directory that
ultimately contains DriveClub's `game.ndx`/`game.dat` and data files.

No VirtualAuto command currently writes here. Any future wrapper must establish
canonical path containment and an unambiguous license policy first.
