# Blender fixtures

This directory may contain only small, original, Git-LFS-tracked fixtures with
adjacent provenance. The current structural smoke fixture is generated at run
time and is not committed.
