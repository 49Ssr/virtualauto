# Runtime manifests

Small schema-backed runtime manifests may be retained here. Generated manifests
must not include usernames, machine names, secret environment variables, or
absolute private paths.

A runtime manifest is metadata about an execution environment. It is not proof
that the rendered result is correct.
