# DriveClubFS input

Expected contents are `game.ndx` plus `game*.dat`, or an older `game.dat` with
an embedded index. Copying is optional: pass an absolute private directory to
the CLI instead when the files already exist elsewhere.
